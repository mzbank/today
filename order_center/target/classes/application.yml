server:
  port: 8095

spring:
  application:
    name: user-center

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8090/eureka/
