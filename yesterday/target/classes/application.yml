server:
  port: 8091

spring:
  application:
    name: yesterday_config
  devtools:
    restart:
      enabled: true
      additional-paths: /src/main/java/
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8090/eureka/